<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="comment">
	
	<!-- 
		*** 댓글 구분 종류
		1 : 타임라인 게시물
		2 : 블로그 페이지
	 -->
	
	<!-- 댓글 입력 -->
	<insert id="insert_comment" parameterType="post_commentVo">
		insert into post_comment
		values(
			comment_code_seq.nextval,
			#{user_id},
			#{comment_contents},
			#{ref_code},
			#{division},
			sysdate
		)
	</insert>
	
	<!-- 댓글 수정 -->
	<update id="update_comment" parameterType="post_commentVo">
		update post_comment
		   set comment_contents = #{comment_contents}
		 where comment_code = #{comment_code}  
	</update>
	
	<!-- 댓글 삭제 -->
	<delete id="delete_comment" parameterType="String">
		delete post_comment
		 where comment_code = #{comment_code}
	</delete>
	
	<!-- 댓글목록 조회 -->
	<select id="select_commentList" parameterType="post_commentVo" resultType="post_commentVo">
		select comment_code, user_id, comment_contents, ref_code, division, comment_date
		  from post_comment
		 where ref_code = #{ref_code}
		   and division = #{division} 
	</select>
</mapper>